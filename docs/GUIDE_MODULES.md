# Гайд по модулям «Решалы»: как писать и как встраивать

Этот документ объясняет, как правильно писать **новые модули** в `modules/*.sh` и аккуратно встраивать их в фреймворк.

Модули — это крупные куски функционала (меню, мастера, сервисные подсистемы), которые запускаются через `run_module` из `reshala.sh`.

---

## 1. Где живут модули и как они подключаются

- Все модули лежат в каталоге:
  - `modules/<имя_модуля>.sh`
- Главный скрипт `reshala.sh` предоставляет функцию-роутер:
  - `run_module <module_name> <function> [args...]`
  - она:
    - делает `source "${SCRIPT_DIR}/modules/<module_name>.sh"`;
    - вызывает указанную функцию внутри модуля с переданными аргументами.
- Любой модуль **обязан** рассчитывать на то, что перед его вызовом уже загружены:
  - `config/reshala.conf` (глобальные `readonly`-константы);
  - `modules/common.sh` (цвета, `menu_header`, `info/ok/warn/err`, `log`, `run_cmd`, `safe_read`, `set_config_var`, `get_config_var`, `view_logs_realtime` и т.д.).

**Никогда** не запускаем модули напрямую (`bash modules/foo.sh`) — только через `run_module`.

---

## 2. Каркас нового модуля

Минимальный скелет файла `modules/my_feature.sh`:

```bash
#!/bin/bash
# ============================================================ #
# ==                 МОДУЛЬ <НАЗВАНИЕ>                      == #
# ============================================================ #
#
# Коротко: что делает модуль, для кого и когда его вызывать.
# Можно в 3–5 строк описать бизнес-логику и важные нюансы.
#
[[ "${BASH_SOURCE[0]}" == "${0}" ]] && exit 1 # Защита от прямого запуска

# === ЛОКАЛЬНЫЕ ХЕЛПЕРЫ (private-функции) =====================

_my_feature_do_something() {
    info "Делаю что-то полезное…"
    # Вся тяжёлая логика здесь. Внешний мир видит только меню.
}

# === ГЛАВНОЕ МЕНЮ МОДУЛЯ =====================================

show_my_feature_menu() {
    while true; do
        clear
        menu_header "<ЗАГОЛОВОК МЕНЮ>"
        echo ""
        echo "   Коротко объясни пользователю, что он может тут сделать."
        echo "   Отметь опасные действия (удаление, форматирование и т.п.)."
        echo ""
        echo "   [1] Первый полезный пункт"
        echo "   [2] Второй пункт"
        echo ""
        echo "   [b] 🔙 Назад"
        echo "------------------------------------------------------"

        local choice; choice=$(safe_read "Твой выбор: " "")

        case "$choice" in
            1) _my_feature_do_something ;;
            # 2) ...
            [bB]) break ;;
            *) printf_error "Нет такого пункта. Смотри в меню, босс."; sleep 1 ;;
        esac
    done
}
```

Главные моменты:

- **Шапка и защитa**:
  - Всегда ставим `#!/bin/bash` и защиту от прямого запуска:
    - `[[ "${BASH_SOURCE[0]}" == "${0}" ]] && exit 1`
- **Структура**:
  - сначала комментарий с описанием модуля,
  - затем «приватные» функции (`_something()`),
  - в конце — одна или несколько «публичных» точек входа вида `show_xxx_menu()`.
- **Именование**:
  - внутренние вспомогательные функции: `_snake_case` с подчёркиванием в начале;
  - публичные меню: `show_<feature>_menu`;
  - язык комментариев и вывода — русский с тем же тоном, что и остальные модули.

---

## 3. Работа с конфигом, логами и правами

### 3.1. Конфиг (`config/reshala.conf`)

- Все глобальные настройки и константы лежат в `config/reshala.conf` и загружены до модулей.
- Для **новых** настроек используй хелперы из `common.sh`:
  - `set_config_var KEY VALUE` — записывает/обновляет значение;
  - `get_config_var KEY`      — возвращает значение (без кавычек).
- Не хардкодь пути и настройки внутри модулей, если это «постоянное» поведение.

Пример:

```bash
# Прочитать предыдущую вместимость ноды, сохранённую speedtest'ом
local capacity
capacity=$(get_config_var "LAST_VPN_CAPACITY")

# Сохранить новый тумблер/значение в конфиг
set_config_var "MY_FEATURE_ENABLED" "1"
```

### 3.2. Логирование

- Главный лог: путь в `LOGFILE` (определён в `config/reshala.conf`).
- Писать в лог нужно только через функцию `log` из `common.sh`:
  - она сама добавляет таймштамп и пишет через `run_cmd tee -a`.

```bash
log "Запускаю сложную операцию в my_feature…"
```

### 3.3. Запуск системных команд (`run_cmd`)

- Любые команды, которые потенциально требуют root/`sudo`, запускаем через `run_cmd`:
  - `run_cmd apt-get update`;
  - `run_cmd systemctl restart ...`;
  - `run_cmd tee /etc/sysctl.d/...`.
- Внутри модулей **не** вызываем `sudo` напрямую.

Если нужно просто вывести в экран без прав — можно вызывать бинарники напрямую (`echo`, `printf`, `awk`, `sed`, `df`, `free` и т.п.).

### 3.4. Сообщения пользователю

- Для всех сообщений на экран используем только алиасы:
  - `info "..."` — обычная инфа;
  - `ok "..."`   — успех;
  - `warn "..."` — предупреждение;
  - `err "..."`  — ошибка (красным, с задержкой 2 сек).

Не нужно изобретать свои `echo "[+] ..."` и жёстко прописанные цвета — всё уже есть.

---

## 4. Меню и UX: как не бесить пользователя

### 4.1. Шапка меню

- В каждом новом меню используй `menu_header` из `common.sh`:

```bash
menu_header "🔧 МОЙ НОВЫЙ МОДУЛЬ"
```

- Под шапкой — 1–2 строки с объяснением:
  - что за меню;
  - какие действия особенно опасны.

### 4.2. Навигация

- Кнопка «Назад» в подменю **всегда** `[b]` / `[B]`.
- Главное меню использует `q/Q` для выхода — не переопределяй это внутри подменю.
- Любые «опасные» действия (удаление, очистка, форматирование) делай с явным подтверждением:

```bash
read -p "Точно снести всё нахуй? (y/n): " confirm
if [[ "$confirm" != "y" ]]; then
    warn "Отмена. Живём дальше."
    return
fi
```

### 4.3. Долгие операции

- Если операция может занять время (docker, apt, network), заранее предупреди:

```bash
printf_warning "Операция может занять до 2–3 минут. Не дёргай клаву."
```

- Для неблокирующих действий (фоновая пересборка кеша, лёгкий опрос) можно использовать `&`, но не превращай модуль в зоопарк фоновых процессов.

---

## 5. Интеграция нового модуля в главное меню

### 5.1. Добавляем файл модуля

1. Кладём `modules/my_feature.sh` с каркасом из раздела выше.
2. Убеждаемся, что файл исполняемый:
   - `chmod +x modules/my_feature.sh` (опционально, так как модуль подключается через `source`, но полезно для единообразия).

### 5.2. Подключаем модуль из `reshala.sh`

Главное меню живёт в `reshala.sh` → функция `show_main_menu`.

Чтобы добавить новую кнопку:

1. В блоке отрисовки меню (`printf "   [N] ..."`) добавляем свою строку, например:
   - `printf "   [6] 🧪 %b\n" "ЭКСПЕРИМЕНТАЛЬНЫЙ МОДУЛЬ"`
2. В обработчике `case "$choice" in` добавляем ветку:
   - `6) run_module my_feature show_my_feature_menu ;;`

Общий паттерн:

- Визуальная строка:
  - человекочитаемое описание на русском;
  - можно добавить пояснение в сером (`${C_GRAY}...${C_RESET}`), как в существующих пунктах.
- Обработчик:
  - **никакой логики в `reshala.sh`**, только делегирование в модуль:
    - вся бизнес-логика остаётся внутри `modules/my_feature.sh`.

---

## 6. Когда нужен отдельный модуль, а когда — виджет или плагин

Перед тем как писать новый файл в `modules/`, ответь на вопросы:

- **Нужно отдельное меню/мастер?**
  - Да → новый модуль в `modules/` + кнопка в `reshala.sh`.
- **Это просто строка в дашборде (метрика)?**
  - Да → новый виджет в `plugins/dashboard_widgets/` (см. `GUIDE_SKYNET_WIDGETS.md`).
- **Это однотипное действие на весь флот серверов?**
  - Да → новый плагин в `plugins/skynet_commands/`.

Так архитектура остаётся чистой: 

- `reshala.sh` — роутер и главное меню;
- `modules/*.sh` — большие фичи и TUI-меню;
- `plugins/dashboard_widgets/*` — маленькие метрики для дашборда;
- `plugins/skynet_commands/*` — массовые действия для SKYNET.

---

## 7. Чек-лист перед тем, как закоммитить модуль

1. **Файл и структура**
   - [ ] Файл лежит в `modules/` и называется понятно (`my_feature.sh`).
   - [ ] Вверху есть `#!/bin/bash` и защита от прямого запуска.
   - [ ] Есть хотя бы одна публичная функция-меню (`show_..._menu`).

2. **Сообщения и цвета**
   - [ ] Все сообщения через `info/ok/warn/err`.
   - [ ] Нет сырых `\033[...]` — только цвета из `common.sh` при необходимости.

3. **Команды и права**
   - [ ] Все опасные/системные действия через `run_cmd`.
   - [ ] Никаких прямых вызовов `sudo`.

4. **Конфиг и состояние**
   - [ ] Новые настройки хранятся через `set_config_var`/`get_config_var`.
   - [ ] Нет хардкода путей, которые уже есть в `config/reshala.conf`.

5. **UX**
   - [ ] В меню используется `menu_header`.
   - [ ] Есть `[b]` для выхода назад.
   - [ ] Опасные операции требуют явного подтверждения.

6. **Интеграция**
   - [ ] В `reshala.sh::show_main_menu` добавлена кнопка.
   - [ ] В `case` добавлен вызов `run_module <module> <entry_func>`.

Если всё из чек-листа сходится — модуль готов к бою и будет вести себя так же предсказуемо, как и остальная «Решала».
