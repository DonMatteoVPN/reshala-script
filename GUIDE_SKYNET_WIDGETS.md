# Гайд: как писать команды для SKYNET и виджеты для дашборда

Этот документ объясняет, как правильно писать свои плагины для:
- SKYNET-команд флота (массовые действия на серверах)
- виджетов дашборда (доп. блоки информации в главном экране "Решалы")

Все примеры даны для текущей структуры проекта и уже настроенных модулей.

---

## 1. Плагины SKYNET (команды для флота)

### 1.1. Где лежат плагины

Все команды для SKYNET находятся в каталоге:

- `plugins/skynet_commands/`

Каждый файл `NN_name.sh` в этой папке — отдельная команда.

SKYNET сам:
- находит эти файлы;
- читает из них заголовок (`# TITLE:`);
- показывает список в меню в **человекочитаемом виде по-русски**;
- по выбору пользователя копирует файл на удалённый сервер, выполняет его там и удаляет.

### 1.2. Минимальная структура файла команды

```bash
#!/bin/bash
# TITLE: Описание команды (по-русски)
# Дополнительный комментарий, что делает команда.

run() {
    # Здесь любая логика на bash.
    echo "Привет с удалённого сервера!"
}

run
```

Требования:
- **Первая строка**: `#!/bin/bash`
- **Вторая строка**: комментарий `# TITLE: ...` — именно этот текст увидит пользователь в меню SKYNET.
- Обязательна функция `run()` и её вызов в конце файла (`run`).

### 1.3. Пример 1 — показать аптайм

Файл: `plugins/skynet_commands/01_get_uptime.sh`

```bash
#!/bin/bash
# TITLE: Показать аптайм сервера
# Плагин для Скайнета: показывает аптайм удалённой машины.

run() {
    echo "Аптайм на сервере:"
    uptime -p
}

run
```

### 1.4. Пример 2 — обновить систему (apt)

Файл: `plugins/skynet_commands/02_update_system.sh`

```bash
#!/bin/bash
# TITLE: Обновить систему (apt update && upgrade)
# Плагин для Скайнета: запускает apt update && apt upgrade.

run() {
    echo "Запускаю обновление системы (apt)..."

    if ! command -v apt-get &>/dev/null; then
        echo "Это не Debian/Ubuntu, пропускаю."
        exit 0
    fi

    apt-get update -qq && apt-get upgrade -y -qq

    echo "Обновление завершено."
}

run
```

### 1.5. Как SKYNET запускает команду

1. Ты в ЦУПе выбираешь:
   - `[c] ☢️  Выполнить команду на флоте`;
   - нужную команду по номеру (по её `TITLE`);
   - режим: на всём флоте или на конкретном сервере.
2. Модуль `skynet.sh`:
   - копирует файл на удалённый сервер в `/tmp/reshala_plugin.sh`;
   - выполняет `bash /tmp/reshala_plugin.sh`;
   - удаляет файл.

Важно:
- На удалённой стороне **НЕ требуется `chmod +x`** — файл запускается через `bash`.
- Всё, что ты выводишь через `echo` внутри плагина, будет видно в терминале ЦУПа.

### 1.6. Рекомендации по написанию SKYNET-плагинов

1. **Будь осторожен с destrutive-командами** (rm, очистка томов и т.п.). Лучше явно спрашивать подтверждение внутри `run()`.
2. **Проверяй окружение**:
   - наличие нужных бинарников (`command -v ...`),
   - тип системы (Debian/Ubuntu или нет),
   - наличие Docker и т.д.
3. **Логируй свои действия** простыми `echo` — так оператор видит, что сейчас происходит на каждом сервере.
4. Имена файлов лучше начинать с двух цифр для сортировки по порядку: `01_`, `02_`, `10_` и т.д.

---

## 2. Виджеты дашборда

### 2.1. Где лежат виджеты

Все виджеты дашборда находятся в каталоге:

- `plugins/dashboard_widgets/`

Каждый файл `NN_name.sh` — отдельный виджет.

Модуль `dashboard.sh` при отрисовке главного экрана:
- читает из конфига список включённых виджетов (`ENABLED_WIDGETS`),
- запускает **только включённые** виджеты через `bash файл.sh`,
- выводит каждую строку виджета в формате `Метка : Значение` с выравниванием.

Управление списком включённых виджетов — через меню:

- `[w] 💡 Управление виджетами дашборда`

(модуль `widget_manager.sh`).

### 2.2. Минимальная структура файла виджета

```bash
#!/bin/bash
# TITLE: Название виджета по-русски
# Краткое описание, что он показывает.

# ВЫВОД ДОЛЖЕН БЫТЬ В ФОРМАТЕ:
#   Заголовок : Значение

echo "Мой параметр : 123"
```

Требования:
- **Первая строка**: `#!/bin/bash`
- **Вторая строка**: `# TITLE: ...` — это имя виджета в меню управления виджетами.
- Вывод: одна или несколько строк вида `Название : Значение` (через двоеточие).

Модуль `dashboard.sh` сам:
- режет строку по первому `:` на `label` и `value`;
- подставляет их в красиво выровненный вывод.

### 2.3. Пример 1 — курс BTC (готовый виджет)

Файл: `plugins/dashboard_widgets/01_crypto_price.sh`

```bash
#!/bin/bash
# TITLE: Курс биткоина (BTC)
# Виджет для дашборда "Решалы": показывает цену BTC.

API_URL="https://api.coingecko.com/api/v3/simple/price?ids=bitcoin&vs_currencies=usd"

if command -v curl &>/dev/null && command -v jq &>/dev/null; then
    PRICE=$(curl -s --connect-timeout 3 "$API_URL" | jq -r '.bitcoin.usd')

    if [[ "$PRICE" =~ ^[0-9]+(\.[0-9]+)?$ ]]; then
        FORMATTED_PRICE=$(printf "%'.0f" "$PRICE")
        echo "Курс BTC : \$${FORMATTED_PRICE}"
    else
        echo "Курс BTC : API Error"
    fi
else
    echo "Курс BTC : curl/jq missing"
fi
```

### 2.4. Пример 2 — нагрузка сервера (load average)

Файл: `plugins/dashboard_widgets/02_load_short.sh`

```bash
#!/bin/bash
# TITLE: Нагрузка сервера (load average)
# Краткий виджет: показывает load average и число ядер.

CORES=$(nproc 2>/dev/null || echo "?")
LOAD=$(uptime 2>/dev/null | awk -F'load average:' '{print $2}' | cut -d',' -f1 | xargs)

if [ -з "$LOAD" ]; then
  echo "Нагрузка : нет данных"
else
  echo "Нагрузка : $LOAD (ядер: $CORES)"
fi
```

### 2.5. Пример 3 — активные SSH-сессии

Файл: `plugins/dashboard_widgets/03_online_users.sh`

```bash
#!/bin/bash
# TITLE: Активные пользователи (ssh/shell)
# Показывает количество активных интерактивных сессий.

COUNT=$(who 2>/dev/null | wc -l | xargs)
if [ -з "$COUNT" ]; then
  echo "Сессии SSH : нет данных"
else
  echo "Сессии SSH : $COUNT"
fi
```

### 2.6. Пример 4 — состояние корневого диска

Файл: `plugins/dashboard_widgets/04_root_disk.sh`

```bash
#!/bin/bash
# TITLE: Диск / (корневая файловая система)
# Показывает использование корневого раздела.

LINE=$(df -h / 2>/dev/null | awk 'NR==2')
USED=$(echo "$LINE" | awk '{print $3 "/" $2}')
PERC=$(echo "$LINE" | awk '{print $5}')

if [ -з "$USED" ]; then
  echo "Диск / : нет данных"
else
  echo "Диск / : $USED ($PERC)"
fi
```

### 2.7. Как включить/выключить виджет

1. Запусти `reshala`.
2. В главном меню выбери `[w] 💡 Управление виджетами дашборда`.
3. Увидишь список вида:

   - `[1] [ВКЛЮЧЕН]  - Курс биткоина (BTC)`
   - `[2] [ВЫКЛЮЧЕН] - Нагрузка сервера (load average)`
   - ...

4. Введи номер виджета, чтобы переключить его состояние (вкл/выкл).
5. При следующей перерисовке дашборда включённые виджеты появятся в блоке `[ WIDGETS ]`.

---

## 3. Общие советы по разработке

1. **Не блокируй дашборд и Skynet**
   - Старайся ставить таймауты (`--connect-timeout` для curl, `timeout` для тяжёлых команд).
   - Не запускай долгие `apt upgrade` прямо в виджете — для этого есть отдельные SKYNET-команды.

2. **Проверяй зависимости**
   - В плагинах и виджетах всегда проверяй наличие нужных бинарников через `command -v`.

3. **Логи и безопасность**
   - SKYNET-плагины выполняются от root (через run_remote). Будь особо аккуратен с `rm`, `docker volume rm` и прочими разрушительными действиями.
   - Лучше явно спрашивать подтверждение (`read -p "Точно? (y/n): "`) внутри таких команд.

4. **Именование**
   - Вся логика для пользователя (то, что он видит в меню) — по-русски.
   - Имена файлов можно оставлять техническими (`01_get_uptime.sh`), главное — прописать человекочитаемый `# TITLE:`.

Если будешь добавлять новые плагины или виджеты — просто следуй этим правилам, и они автоматически появятся в меню SKYNET и дашборде без каких-либо дополнительных правок кода.
