#!/bin/bash
# ============================================================ #
# ==      ИНСТРУМЕНТ «РЕШАЛА» v2.441 - Skynet Framework       == #
# ============================================================ #
#
# Точка входа. Этот скрипт — прораб. Он только отдаёт команды
# модулям и отрисовывает главное меню.
#
set -uo pipefail

# Определяем РЕАЛЬНОЕ местоположение скрипта, даже если он запущен через симлинк
SOURCE="${BASH_SOURCE[0]}"
while [ -h "$SOURCE" ]; do # Раскручиваем все симлинки
  DIR="$( cd -P "$( dirname "$SOURCE" )" && pwd )"
  SOURCE="$(readlink "$SOURCE")"
  [[ $SOURCE != /* ]] && SOURCE="$DIR/$SOURCE"
done
readonly SCRIPT_DIR="$( cd -P "$( dirname "$SOURCE" )" && pwd )"

readonly VERSION="v2.441" # Версия фреймворка

# ============================================================ #
#              ПОДГОТОВКА И ЗАГРУЗКА КОМПОНЕНТОВ               #
# ============================================================ #

# Загружаем конфигурацию. Если её нет - всё, пиздец, приехали.
if [ -f "${SCRIPT_DIR}/config/reshala.conf" ]; then
    source "${SCRIPT_DIR}/config/reshala.conf"
else
    echo "[FATAL ERROR] Configuration file config/reshala.conf not found."
    exit 1
fi

# Загружаем общие инструменты. Без них как без рук.
if [ -f "${SCRIPT_DIR}/modules/common.sh" ]; then
    source "${SCRIPT_DIR}/modules/common.sh"
else
    echo "[FATAL ERROR] Common tools module modules/common.sh not found."
    exit 1
fi

# ============================================================ #
#                     ГЛАВНАЯ ЛОГИКА                           #
# ============================================================ #

# Универсальный загрузчик и запускатор модулей
run_module() {
    local module_name="$1"
    shift # Убираем имя модуля, оставляем только имя функции и её аргументы
    local module_path="${SCRIPT_DIR}/modules/${module_name}.sh"

    if [ -f "$module_path" ]; then
        # Подключаем модуль и вызываем нужную функцию с её аргументами
        source "$module_path"
        "$@" # "$@" теперь содержит имя функции и её параметры
    else
        log "Критическая ошибка: модуль '${module_name}' не найден."
        printf_error "Модуль '${module_name}' отсутствует. Установка повреждена."
    fi
}

# Главное меню, которое видит пользователь
# Главное меню, которое видит пользователь (ФИНАЛЬНАЯ ВЕРСИЯ v4.3)
show_main_menu() {
    trap 'printf "\r\033[K%b" "${C_RED}🛑 Куда собрался? Жми [q], чтобы выйти!${C_RESET}"; sleep 1' SIGINT

    while true; do
        # Сначала отрисовка дашборда
        run_module dashboard show
        
        # Баннер обновления, если есть
        if [[ ${UPDATE_AVAILABLE:-0} -eq 1 ]]; then
            printf "\n%b‼️ ДОСТУПНО ОБНОВЛЕНИЕ! %s -> %s ‼️%b\n" "${C_BOLD}${C_RED}" "${VERSION}" "${LATEST_VERSION}" "${C_RESET}"
        fi
        
        printf "\n%s\n\n" "Чё делать будем, босс?"
        
        # --- ПУНКТЫ МЕНЮ В ПРАВИЛЬНОМ ПОРЯДКЕ ---

        # [0] Управление флотом (только на главном сервере)
        if [ "${SKYNET_MODE:-0}" -ne 1 ]; then
            printf "   [0] 🌐 %b\n" "УПРАВЛЕНИЕ ФЛОТОМ ${C_WHITE}(Skynet Mode)${C_RESET}"
            echo "   ---------------------------------------------------"
        fi
        
        # [1] Сервисное меню
        printf "   [1] 🔧 %b\n" "СЕРВИСНОЕ МЕНЮ ${C_GRAY}(Обновы, Сеть, Тесты)${C_RESET}"
        # [2] Быстрые логи
        printf "   [2] 📜 %b\n" "БЫСТРЫЕ ЛОГИ ${C_GRAY}(Решала, Панель, Нода, Бот)${C_RESET}"
        # [3] Управление Docker
        printf "   [3] 🐳 %b\n" "УПРАВЛЕНИЕ DOCKER ${C_GRAY}(Мусорка, Инфо)${C_RESET}"
        echo ""
        # [4] Установка Панели
        printf "   [4] 💿 %b\n" "УСТАНОВИТЬ ПАНЕЛЬ REMNAWAVE ${C_GRAY}(High-Load)${C_RESET}"
        # [5] Установка Бота
        printf "   [5] 🤖 %b\n" "УСТАНОВИТЬ БОТА BEDALAGA ${C_GRAY}(Coming Soon)${C_RESET}"
        echo ""

        # [w] УПРАВЛЕНИЕ ВИДЖЕТАМИ (НАША НОВАЯ ФИЧА)
        printf "   [w] 💡 %b\n" "Управление виджетами дашборда"

        # [u] Обновление Решалы (появляется при необходимости)
        if [[ ${UPDATE_AVAILABLE:-0} -eq 1 ]]; then
            printf "   %b[u] ‼️ ОБНОВИТЬ РЕШАЛУ ‼️%b\n" "${C_BOLD}${C_YELLOW}" "${C_RESET}"
        fi
        echo ""

        # Контекстные кнопки [d] и [q]
        if [ "${SKYNET_MODE:-0}" -eq 1 ]; then
            printf "   [d] 🗑️  Снести Решалу нахуй (Удаление)\n"
            printf "   [q] 🔙 %b\n" "${C_CYAN}ВЕРНУТЬСЯ В ЦУП${C_RESET}"
        else
            printf "   [d] 🗑️  Снести Решалу нахуй (Удаление)\n"
            printf "   [q] 🚪 Свалить (Выход)\n"
        fi
        echo "------------------------------------------------------"
        
        local choice; read -r -p "Твой выбор, босс: " choice || continue
        
        # --- ОБРАБОТЧИК НАЖАТИЙ (ПОЛНЫЙ) ---
        case "$choice" in
            0) if [ "${SKYNET_MODE:-0}" -ne 1 ]; then run_module skynet show_fleet_menu; else printf_error "Ты уже в матрице."; fi ;;
            1) run_module local_care show_maintenance_menu ;;
            2) run_module diagnostics show_diagnostics_menu ;;
            3) run_module diagnostics show_docker_menu ;; # Теперь отдельное докер-меню
            4) printf_warning "Модуль установки панели в разработке." && sleep 2 ;;
            5) printf_warning "Модуль установки бота в разработке." && sleep 2 ;;
            w|W) run_module widget_manager show_widgets_menu ;; # НАША НОВАЯ ФИЧА
            u|U) if [[ ${UPDATE_AVAILABLE:-0} -eq 1 ]]; then run_module self_update run_update; else printf_error "Обновлений нет."; fi ;;
            d|D) run_module self_update uninstall_script ;;
            q|Q)
                trap - SIGINT
                if [ "${SKYNET_MODE:-0}" -eq 1 ]; then
                    exit 0
                else
                    echo "Был рад помочь. Не обосрись. 🥃"
                    exit 0
                fi
                ;;
            *) printf_error "Нет такого пункта." ;;
        esac
    done
}

# ============================================================ #
#                       ТОЧКА ВХОДА                            #
# ============================================================ #
main() {
    # Инициализируем логгер
    init_logger

    # Если мы не root, дальнейший разговор бессмысленен
    if [[ $EUID -ne 0 ]]; then
        printf_error "Этот скрипт должен быть запущен от имени root или через sudo."
        exit 1
    fi

    # Обработка команды `install` - это особый случай
    if [[ "${1:-}" == "install" ]]; then
        # Логика установки будет в self_update.sh, но вызывается так
        # Мы уже под root, так что можем просто вызывать
        source "${SCRIPT_DIR}/modules/self_update.sh" && install_script
        
        # После установки, ПРОВЕРЯЕМ и СТАВИМ sudo, если его нет.
        # Это одна из первых вещей, которую Решала делает для сервера.
        if ! command -v sudo &>/dev/null; then
            if command -v apt-get &>/dev/null; then
                printf_info "Команда 'sudo' не найдена. Устанавливаю..."
                apt-get update -qq >/dev/null
                apt-get install -y -qq sudo
                printf_ok "'sudo' установлен. Это правильно."
            fi
        fi
        exit 0
    fi

    log "Запуск фреймворка Решала ${VERSION}"

    # Первичная проверка обновлений (без фона, чтобы переменные дошли до меню)
    run_module self_update check_for_updates

    # Показываем главное меню
    show_main_menu
}

# Запускаем всю эту шарманку
main "$@"